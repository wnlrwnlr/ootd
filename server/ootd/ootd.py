class OOTD:
    def __init__(self, api_key):
        self.api_key = api_key
        self.cache = Cache()
    
    def handle(params):
        #param 이용해서 
        #1. cache 확인
        #2. cache에 내용 존재 -> return
        #3. cache에 내용 존재X -> api 호출, return
        return

class Cache:
    def __init__(self):
        return